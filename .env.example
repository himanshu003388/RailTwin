# RailTwin Backend Configuration

# Gemini AI (required for copilot)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override the default Gemini model (gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.5-flash

# OpenWeatherMap (live weather fallback — app works without it via Open-Meteo)
# Get a free API key at https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweathermap_api_key_here

# JWT Secret (change in production)
JWT_SECRET=railtwin-secret-key-change-in-production
